export function getInstagramDeliveryDescription(): string {
  return 'Publishes the approved row directly to Instagram from the Worker using the stored professional account credentials.';
}

export function getInstagramDeliveryHint(): string {
  return 'Instagram publishing requires a selected image and uses the approved text as the post caption.';
}