{
  "name": "linkedin-bot-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --env local",
    "dev:default": "wrangler dev",
    "dev:remote": "wrangler dev --env local --remote",
    "deploy": "wrangler deploy --env \"\"",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260317.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.9.1"
  }
}