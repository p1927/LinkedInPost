export function getLinkedInDeliveryDescription(): string {
  return 'Publishes the approved row directly to LinkedIn from the Worker using the stored member credentials.';
}

export function getLinkedInDeliveryHint(): string {
  return 'This direct integration publishes the approved text and uploads the selected image when one is attached.';
}